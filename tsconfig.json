{
  "compilerOptions": {
    /* === Core Settings === */
    "target": "ES2020",                    
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,                        
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,                        

    /* === Module Resolution === */
    "module": "esnext",
    "moduleResolution": "node",         
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "jsx": "preserve",
    "incremental": true,

    /* === Path Aliases (adjust if needed) === */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    /* === Lint / Cleanup Helpers === */
    "noUnusedLocals": true,                // Warn about unused variables
    "noUnusedParameters": true,            // Warn about unused function params
    "verbatimModuleSyntax": false,          // Preserve import syntax (replaces importsNotUsedAsValues)

    /* === Type Checking Improvements === */
    "strictNullChecks": true,
    "noImplicitAny": true,
    "noFallthroughCasesInSwitch": true,
    "allowSyntheticDefaultImports": true
  },

  /* === Project Structure === */
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules",
    ".next",
    "out",
    "dist",
    "build"
  ]
}
